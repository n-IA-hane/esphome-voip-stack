# Source

Extracted from `n-IA-hane/esphome-intercom`.

This repository contains the standalone ESPHome `voip_stack` component plus the
small `ring_buffer` component it needs for CAPS-aware RTP/audio buffering.

The Home Assistant integration, Lovelace card, ready product YAMLs, voice
assistant packages and full device examples remain in
[`esphome-intercom`](https://github.com/n-IA-hane/esphome-intercom).

